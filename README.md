# two-dimensional-array

#include<stdio.h>
int main(){
int rows,cols,i,j;
printf("number of rows\n");
scanf("%d",&rows);
printf("number of cols:\n");
scanf("%d",&cols);
int arr[rows][cols];
printf("enter arrays elements:\n);
for(i=0;i<n;i++){
for(j=0;j<cols;j++){
scanf("%d",arr[i][j]);
}
}
printf("array elements are\n");
for(i=0;i<rows;i++){
for(j=0;j<cols;j++){
printf("elements are:%d,arr[i][j]);
}
printf("\n");
}
return 0;
}
